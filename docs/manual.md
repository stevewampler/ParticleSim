# ParticleSim — User Manual

Status: not started. This will document how to use the simulator, with
examples, once there's something real to run. Content here should only
ever describe behavior that's actually been implemented and tested — see
`todo/requirements.md` for the design/intent, and `todo/TODO.md` for what's
built so far.

Planned to start once Phase 2 or 3 (`todo/TODO.md`) produces a runnable
example — the physics core, or the flag worked example.
